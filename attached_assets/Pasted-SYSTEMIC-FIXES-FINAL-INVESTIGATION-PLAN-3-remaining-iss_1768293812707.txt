SYSTEMIC FIXES — FINAL INVESTIGATION PLAN

3 remaining issues. Trace each failure through the full pipeline:
Chunking → Extraction → Staging → Promotion → Query

================================================================================
ISSUE 1: CLOUDAI — CHUNK/EXTRACTION PROBLEM
================================================================================

CloudAI is in source doc (lines 62-113, "BREAKOUT COMPANIES") but not extracted.

STEP 1: CHECK CHUNKING
```sql
-- Get document ID
SELECT id, name FROM documents 
WHERE tenant_id = (SELECT id FROM vaults WHERE name = 'Launchpad Ventures')
  AND name ILIKE '%portfolio%';

-- Check chunk sizes and content
SELECT c.chunk_index, LENGTH(c.content) as chars, LEFT(c.content, 150) as preview
FROM chunks c
WHERE c.document_id = '<DOC_ID>'
ORDER BY chunk_index;

-- Find BREAKOUT COMPANIES header
SELECT chunk_index, LEFT(content, 300)
FROM chunks c
WHERE c.document_id = '<DOC_ID>'
  AND content ILIKE '%breakout%';

-- Find CloudAI content
SELECT chunk_index, LEFT(content, 300)
FROM chunks c
WHERE c.document_id = '<DOC_ID>'
  AND content ILIKE '%cloudai%';
```

STEP 2: CHECK EXTRACTION (was it extracted at all?)
```sql
-- Check staging
SELECT * FROM staging.entities
WHERE name ILIKE '%cloudai%';

-- Check for name variants (Cloud AI, CloudAI Inc.)
SELECT * FROM entities
WHERE tenant_id = (SELECT id FROM vaults WHERE name = 'Launchpad Ventures')
  AND (name ILIKE '%cloud%ai%' OR name ILIKE '%cloud ai%');
```

STEP 3: CHECK EXTRACTION LOGS (if they exist)
```sql
SELECT * FROM extraction_logs
WHERE content ILIKE '%cloudai%';
```

ROOT CAUSE DETERMINATION:
- Header in chunk N, content in chunk N+1 → Chunk overlap too small
- CloudAI not in any chunk → Chunking completely missed section
- In chunk but not in staging → LLM extraction missed it
- In staging but not in KG → Promotion/routing issue
- In KG with different name → Deduplication issue

================================================================================
ISSUE 2: CFO RELATIONSHIP — POST-PROCESSOR / EXTRACTION
================================================================================

Thomas Bradley (PERSON) and Chief Financial Officer (ROLE) both exist.
No HOLDS_POSITION relationship between them.
Source: "Thomas Bradley - Chief Financial Officer"

STEP 1: DO HOLDS_POSITION PATTERNS EXIST?
```bash
grep -n "HOLDS_POSITION\|ROLE_PATTERN\|Chief\|Officer\|position" \
  src/context_foundry/extraction/post_processor.py
```

(Post-processor may only have INVESTED_IN patterns, not HOLDS_POSITION)

STEP 2: CHECK STAGING FOR RELATIONSHIP
```sql
-- Was relationship extracted but not promoted?
SELECT * FROM staging.relationships
WHERE tenant_id = (SELECT id FROM vaults WHERE name = 'Morrison & Sterling')
  AND (source_name ILIKE '%bradley%' OR target_name ILIKE '%bradley%');

-- Check for HOLDS_POSITION relationships in staging
SELECT * FROM staging.relationships
WHERE relation_type = 'HOLDS_POSITION'
  AND tenant_id = (SELECT id FROM vaults WHERE name = 'Morrison & Sterling');
```

STEP 3: CHECK ENTITY NAME FORMATS
```sql
-- How is Thomas Bradley stored?
SELECT id, name, entity_type FROM entities
WHERE tenant_id = (SELECT id FROM vaults WHERE name = 'Morrison & Sterling')
  AND name ILIKE '%bradley%';

-- How is CFO stored?
SELECT id, name, entity_type FROM entities
WHERE tenant_id = (SELECT id FROM vaults WHERE name = 'Morrison & Sterling')
  AND (name ILIKE '%CFO%' OR name ILIKE '%Chief Financial%' OR name ILIKE '%financial officer%');
```

STEP 4: TEST PATTERN MANUALLY
```python
import re

text = "Thomas Bradley - Chief Financial Officer"

patterns = [
    r"([A-Z][a-z]+ [A-Z][a-z]+)\s*-\s*(Chief Financial Officer)",
    r"([A-Z][a-z]+ [A-Z][a-z]+)\s*[-–—]\s*(CFO|Chief Financial Officer)",
]

for p in patterns:
    match = re.search(p, text)
    print(f"Pattern: {p}")
    print(f"Match: {match.groups() if match else None}")
```

ROOT CAUSE DETERMINATION:
- No HOLDS_POSITION patterns in post-processor → Add patterns
- Patterns exist but wrong regex → Fix hyphen variants (- – —)
- In staging but not promoted → Migration 016 should have fixed this
- Not in staging → LLM extraction missed it

================================================================================
ISSUE 3: NON-CTOS AS CTOS — QUERY LOGIC
================================================================================

Query "Who is the CTO?" returns Robert Chen, Elizabeth Warren (Independent Directors).
These are NOT CTOs in the knowledge graph.

HYPOTHESIS: Query uses embedding similarity fallback instead of relationship filtering.

STEP 1: CHECK ROBERT CHEN'S ACTUAL ROLE IN KG
```sql
SELECT e.name, r.relationship_type, e2.name as role_or_target
FROM entities e
LEFT JOIN relationships r ON e.id = r.source_entity_id
LEFT JOIN entities e2 ON r.target_entity_id = e2.id
WHERE e.tenant_id = (SELECT id FROM vaults WHERE name = 'TechVentures')
  AND e.name = 'Robert Chen';
```

STEP 2: CHECK ALL HOLDS_POSITION RELATIONSHIPS IN TECHVENTURES
```sql
SELECT e.name as person, r.relationship_type, e2.name as role
FROM entities e
JOIN relationships r ON e.id = r.source_entity_id
JOIN entities e2 ON r.target_entity_id = e2.id
WHERE r.tenant_id = (SELECT id FROM vaults WHERE name = 'TechVentures')
  AND r.relationship_type IN ('HOLDS_POSITION', 'HAS_ROLE');
```

STEP 3: CHECK WHO ACTUALLY HAS CTO ROLE
```sql
SELECT e.name as person, e2.name as role
FROM entities e
JOIN relationships r ON e.id = r.source_entity_id
JOIN entities e2 ON r.target_entity_id = e2.id
WHERE r.tenant_id = (SELECT id FROM vaults WHERE name = 'TechVentures')
  AND r.relationship_type IN ('HOLDS_POSITION', 'HAS_ROLE')
  AND (e2.name ILIKE '%CTO%' OR e2.name ILIKE '%Chief Technology%');
```

STEP 4: CHECK QUERY LOGIC
```bash
# Find query resolution logic
grep -rn "role\|position\|CTO\|semantic\|embedding\|similarity" \
  src/context_foundry/query/ --include="*.py" | head -30
```

ROOT CAUSE DETERMINATION:
- Robert Chen has no HOLDS_POSITION relationship → Query falls back to semantic search
- Robert Chen has HOLDS_POSITION = "Independent Director" → Query logic doesn't filter by role name
- Query uses embedding similarity as primary, not fallback → Fix query to use relationships first

FIX:
```python
# Pseudocode for correct query logic
def query_role(role_name, vault_id):
    # Step 1: Try exact relationship match
    results = db.query("""
        SELECT person.name 
        FROM entities person
        JOIN relationships r ON person.id = r.source_entity_id
        JOIN entities role ON r.target_entity_id = role.id
        WHERE r.relationship_type IN ('HOLDS_POSITION', 'HAS_ROLE')
          AND (role.name ILIKE %s OR role.name ILIKE %s)
    """, [f'%{role_name}%', f'%{expand_acronym(role_name)}%'])
    
    if results:
        return results
    
    # Step 2: If no relationship match, say so (don't guess)
    return f"No {role_name} found in knowledge graph"
```

================================================================================
VERIFICATION (After All Fixes)
================================================================================

1. Delete all test vaults:
```sql
DELETE FROM vaults WHERE name IN (
  'TechVentures', 'Launchpad Ventures', 'Morrison & Sterling',
  'Titan Manufacturing', 'Riverside Medical Center'
);
```

2. Re-run full E2E:
```bash
python scripts/e2e_lifecycle_test.py
```

3. Manually verify specific queries:
```bash
# Test CloudAI
curl -X POST http://localhost:8000/api/query \
  -d '{"vault": "Launchpad Ventures", "query": "Tell me about CloudAI"}'

# Test CFO
curl -X POST http://localhost:8000/api/query \
  -d '{"vault": "Morrison & Sterling", "query": "Who is the CFO?"}'

# Test CTO (should NOT return Independent Directors)
curl -X POST http://localhost:8000/api/query \
  -d '{"vault": "TechVentures", "query": "Who is the CTO?"}'
```

4. Run regression gate:
```bash
python scripts/regression_gate_test.py
```

================================================================================
REPORT FORMAT
================================================================================

For each issue:
1. Root cause (with SQL evidence)
2. Where in pipeline it failed (Chunking/Extraction/Staging/Promotion/Query)
3. Proposed fix (specific code change)
4. Test case to prevent regression
5. Files changed

================================================================================