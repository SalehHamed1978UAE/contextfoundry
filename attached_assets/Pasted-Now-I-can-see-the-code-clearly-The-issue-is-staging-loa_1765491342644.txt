Now I can see the code clearly. The issue is:

**`staging_loader.py` has no chunk_id** — it only has `source_document_id`. EntityMention requires `chunk_id` to link entities to specific text locations.

**And EntityMention is not imported.**

---

## Replit Instructions: Add EntityMention Creation to Extraction

### Step 1: Check if ExtractedEntity has chunk_id

```bash
grep -A 20 "class ExtractedEntity" src/context_foundry/extraction/entity_extractor.py
```

If `source_chunk_id` exists → proceed to Step 3
If not → do Step 2 first

### Step 2: Add chunk_id to ExtractedEntity (if missing)

In `src/context_foundry/extraction/entity_extractor.py`, add to the dataclass:

```python
@dataclass
class ExtractedEntity:
    canonical_name: str
    entity_type: str
    # ... existing fields ...
    source_document_id: Optional[str] = None
    source_chunk_id: Optional[str] = None  # ADD THIS
    source_span: Optional[str] = None
```

Then find where ExtractedEntity is instantiated and pass chunk_id.

### Step 3: Update staging_loader.py

**Add import** (around line 8):
```python
from ..models.schema import (
    Entity, Relationship, LifecycleState, EntityMention  # ADD EntityMention
)
```

**In `load_entity()` method**, after the entity is created (around line 207, after `self.session.add(entity)`):

```python
        self.session.add(entity)
        
        # Create EntityMention to link entity to chunk
        if extracted.source_chunk_id:
            mention = EntityMention(
                id=uuid.uuid4(),
                entity_id=entity.id,
                document_id=uuid.UUID(extracted.source_document_id) if extracted.source_document_id else None,
                chunk_id=uuid.UUID(extracted.source_chunk_id),
                tenant_id=uuid.UUID(self.tenant_id) if self.tenant_id else None,
                mention_text=extracted.source_span or extracted.canonical_name,
                confidence=extracted.confidence,
            )
            self.session.add(mention)
```

**Also add mention creation for "updated" case** (around line 185):
```python
            # When updating, also ensure mention exists
            if extracted.source_chunk_id:
                existing_mention = self.session.query(EntityMention).filter_by(
                    entity_id=existing.id,
                    chunk_id=uuid.UUID(extracted.source_chunk_id)
                ).first()
                
                if not existing_mention:
                    mention = EntityMention(
                        id=uuid.uuid4(),
                        entity_id=existing.id,
                        document_id=uuid.UUID(extracted.source_document_id) if extracted.source_document_id else None,
                        chunk_id=uuid.UUID(extracted.source_chunk_id),
                        tenant_id=uuid.UUID(self.tenant_id) if self.tenant_id else None,
                        mention_text=extracted.source_span or extracted.canonical_name,
                        confidence=extracted.confidence,
                    )
                    self.session.add(mention)
                    
            return existing, "updated"
```

### Step 4: Trace chunk_id availability

```bash
# Where is extraction called from? Does chunk_id exist there?
grep -rn "entity_extractor\|extract.*entity\|load_entity" src/context_foundry/ --include="*.py" | head -30
```

We need to ensure chunk_id flows from the extraction pipeline → ExtractedEntity → StagingLoader → EntityMention.

### Step 5: Test

After changes:
1. Extract one document
2. Check EntityMention records exist
3. Verify RE Agent can find the new entities

Report what Step 1 shows (does ExtractedEntity have chunk_id?) and I'll adjust instructions if needed.