
  # ═══════════════════════════════════════════════════════════════════
  # EXECUTION ONLY — NO CODE CHANGES, NO TROUBLESHOOTING, NO DEVIATION
  # ═══════════════════════════════════════════════════════════════════

  set -euo pipefail

  NEW=1d33909b-754e-4dcc-86b4-4cfb0023a6a2
  OLD=4668fc6d-c401-46d2-9797-4c8785f4d6ce
  QSET=67c09370-b458-4717-8244-2caa52ff6475

  echo "═══ STEP A: Gardener (promote STAGING → TRUSTED) ═══"
  curl -s -X POST http://localhost:5000/api/gardener/run | jq

  echo ""
  echo "═══ STEP B: Re-extract NEW vault with patched code ═══"
  python3 -u scripts/run_vault_extraction.py --vault-id "$NEW" --use-ontology
  2>&1 | tee /tmp/new_extract.log

  echo ""
  echo "═══ STEP C: Post-extraction gates ═══"

  echo "=== Documents ==="
  psql "$DATABASE_URL" -c "SELECT COUNT(*) AS docs FROM platform.documents 
  WHERE tenant_id='$NEW'::uuid;"

  echo "=== Chunks ==="
  psql "$DATABASE_URL" -c "SELECT COUNT(*) AS chunks FROM 
  public.document_chunks WHERE tenant_id='$NEW'::uuid;"

  echo "=== Entities ==="
  psql "$DATABASE_URL" -c "SELECT COUNT(*) AS entities FROM public.entities 
  WHERE tenant_id='$NEW'::uuid;"

  echo "=== Relationships ==="
  psql "$DATABASE_URL" -c "SELECT COUNT(*) AS rels FROM public.relationships 
  WHERE tenant_id='$NEW'::uuid;"

  echo "=== Lifecycle state distribution ==="
  psql "$DATABASE_URL" -c "SELECT lifecycle_state, COUNT(*) FROM 
  public.entities WHERE tenant_id='$NEW'::uuid GROUP BY lifecycle_state;"

  echo "=== Relationship type distribution (top 25) ==="
  psql "$DATABASE_URL" -c "SELECT relationship_type, COUNT(*) FROM 
  public.relationships WHERE tenant_id='$NEW'::uuid GROUP BY relationship_type
   ORDER BY COUNT(*) DESC LIMIT 25;"

  echo ""
  echo "═══ DECISION GATE ═══"
  echo "Check the above output:"
  echo "  - Relationships should be 950-1064"
  echo "  - Relationship types should be 18-22"
  echo "  - Should see MEMBER_OF, MANAGES, LEADS in top 25"
  echo ""
  echo "If gates PASS, run 100Q test:"
  echo "  python3 -m src.test_runner.runner --vault-id \"$NEW\" 
  --question-set-id \"$QSET\" --mode auto 2>&1 | tee /tmp/eval_new.log"
  echo ""
  echo "If gates FAIL, STOP and paste the output back to Claude."

