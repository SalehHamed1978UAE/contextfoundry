 📋 INSTRUCTIONS FOR REPLIT (Give These Exactly)

  ---
  🎯 REPLIT: Execute Phase 3 Ontology Validation

  You have the code. Now execute the complete Phase 3 workflow.

  ---
  STEP 1: Read the Work Plan

  cat docs/PHASE3_REPLIT_WORKPLAN.md

  Read this entire document. It explains:
  - What each validation gate checks
  - What success looks like
  - How to handle failures

  ---
  STEP 2: Run Pre-Flight Checks

  Execute these 4 checks. ALL MUST PASS before proceeding:

  # Check 1: Scripts exist
  ls -la scripts/phase3_ontology_validator.py
  scripts/phase3_ontology_execution.py

  # Check 2: Corpus exists
  ls -la "test documents/ClaudeCode_NExus_Industries_corpus/" | head -20

  # Check 3: Database connection
  python -c "import os; db = os.environ.get('DATABASE_URL'); print('✅ DB 
  configured' if db else '❌ DB NOT SET')"

  # Check 4: Platform running
  curl -s http://localhost:5000/api/health && echo " ✅ Platform running" ||
  echo "❌ Platform NOT running - start it first"

  If ANY check fails: STOP and report which check failed.

  If ALL checks pass: Proceed to Step 3.

  ---
  STEP 3: Execute Phase 3 Workflow

  Run this single command. It will take 60-90 minutes:

  python scripts/phase3_ontology_execution.py 2>&1 | tee phase3_execution.log

  What this does:
  1. Creates fresh vault: "ClaudeCode Nexus Industries (Ontology)"
  2. Uploads 100+ documents from corpus
  3. Runs ONTOLOGY extraction (--use-ontology flag)
  4. Runs 100-question test
  5. Runs 9-gate deterministic validation

  While it runs:
  - Watch for errors in the output
  - Do NOT interrupt the process
  - Do NOT skip ahead if something looks slow

  Expected time per stage:
  - Vault creation: 2 minutes
  - Document upload: 5-10 minutes
  - Ontology extraction: 30-60 minutes ⏰ (LONGEST STAGE)
  - 100Q test: 10-15 minutes
  - Validation: 1 minute

  ---
  STEP 4: Verify Success

  After the script completes, run these verification commands:

  # Verification 1: Check exit code
  echo "Exit code: $?"  # Must be 0 for success

  # Verification 2: Check overall pass status
  python -c "import json; result = 
  json.load(open('data/phase3_validation_results.json')); print('✅ OVERALL 
  PASS' if result['overall_pass'] else '❌ OVERALL FAIL')"

  # Verification 3: Check which gates passed
  python -c "
  import json
  result = json.load(open('data/phase3_validation_results.json'))
  print('GATE RESULTS:')
  for gate, data in result['gates'].items():
      status = '✅' if data['passed'] else '❌'
      print(f'{status} {gate}: {data[\"message\"]}')"

  # Verification 4: Check accuracy
  python -c "import json; d=json.load(open('data/test-runner/status.json')); 
  print(f\"Accuracy: {d['qa_progress']['accuracy_percent']}% 
  ({d['qa_progress']['passed']}/{d['qa_progress']['total']} passed)\")"

  # Verification 5: Extract vault ID
  python -c "import json; print('Vault ID:', 
  json.load(open('data/phase3_validation_results.json'))['vault_id'])"

  ---
  STEP 5: Report Results

  If ALL verifications pass:

  Report this exact format:

  ✅ PHASE 3 COMPLETE - ALL VALIDATION GATES PASSED

  Vault ID: [paste vault ID from verification 5]
  Vault Name: ClaudeCode Nexus Industries (Ontology)
  Accuracy: [paste accuracy from verification 4]
  Baseline: 88% (multi-model)
  Result: [MATCHES / EXCEEDS / FALLS SHORT OF] baseline

  Gate Results:
  [paste all gate results from verification 3]

  Recommendation: [YES / NO] - Make ontology pipeline the default

  Files Generated:
  - data/phase3_validation_results.json
  - data/test-runner/status.json
  - phase3_execution.log

  If ANY verification fails:

  Report this format:

  ❌ PHASE 3 FAILED

  Failed at: [which step/gate]
  Error: [error message]
  Exit code: [exit code]

  Failed gates:
  [paste failed gates from verification 3]

  Log excerpt:
  [paste last 50 lines of phase3_execution.log]

  ---
  SUCCESS CRITERIA - ALL MUST BE TRUE:

  Before declaring success, verify:

  - Exit code = 0
  - phase3_validation_results.json shows "overall_pass": true
  - All 9 gates show "passed": true
  - Accuracy ≥ 88.0%
  - 100/100 questions answered
  - Vault ID recorded in results file

  ONLY if ALL checkboxes are checked: Declare success.

  ---
  CRITICAL RULES:

  1. ❌ DO NOT skip steps - Each builds on the previous
  2. ❌ DO NOT fake validation - Every gate checks the database
  3. ❌ DO NOT declare success early - Wait for all 9 gates
  4. ✅ DO capture all output - Save logs if anything fails
  5. ✅ DO verify before reporting - Run all 5 verifications

  ---
  FAILURE HANDLING:

  If the script fails:

  # Capture the error
  tail -100 phase3_execution.log > error_report.txt

  # Check which gate failed
  cat data/phase3_validation_results.json | python -m json.tool

  # Report the failure (don't try to fix it yourself)

  Then STOP and report to the user.

  ---
  🎯 EXPECTED OUTPUT (Success Case):

  [STEP 1] Creating fresh vault...
    ✅ Authenticated
    ✅ Created vault: abc-123-def
    ✅ Vault name: ClaudeCode Nexus Industries (Ontology)

  [STEP 2] Collecting and uploading documents...
    Found 147 documents to upload
    ✅ Uploaded: 147, Failed: 0

  [STEP 3] Running ONTOLOGY extraction (--use-ontology)...
    This will take 30-60 minutes...
    ✅ Extraction completed

  [STEP 4] Running 100-question test...
    This will take 10-15 minutes...
    ✅ Test completed

  [STEP 5] Running deterministic validation...
    GATE 1: Checking vault exists...
    ✅ PASS: Vault exists: ClaudeCode Nexus Industries (Ontology)
    GATE 2: Checking document count...
    ✅ PASS: 147 documents uploaded (required: 100)
    ... [continues for all 9 gates] ...
    ✅ ALL GATES PASSED - PHASE 3 VALIDATION SUCCESS

  ✅ PHASE 3 COMPLETE - ALL VALIDATION GATES PASSED

  ---
  🚀 BEGIN EXECUTION

  Start with Step 1 (read work plan) and proceed sequentially through all
  steps.

  Estimated total time: 60-90 minutes

  You have everything you need. Execute now.