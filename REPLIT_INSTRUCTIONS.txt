STAGE 3A — REPLIT INSTRUCTIONS
================================

On Replit, click the Shell tab (bottom panel).
Ignore the git sync screen. Type this ONE line:

git fetch origin && git reset --hard origin/stage3a-property-plane-mvp

That's it. It force-syncs to the branch, no merge,
no conflict, no sync screen. Everything gets overwritten
to match the remote branch exactly.

After that finishes, type:

bash scripts/stage3a_step1_inventory.sh

Then if coverage is 12/16 or higher:

bash scripts/stage3a_step2_migrate_and_validate.sh
