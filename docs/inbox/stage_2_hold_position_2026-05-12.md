# Stage 2 Readiness — Hold Position for Design Conversation

## Alignment

- **Module touched:** none; hold-position instruction only
- **Product vs implementation:** product/design checkpoint before further engineering
- **Architecture-doc consistency:** follows Stage 2 Readiness Promotion Validation: scoped extraction, tenant isolation, verification, and promotion now work on clean data; default activation still requires product/governance decisions
- **Drift risk:** converting product/governance choices into engineering defaults before explicit Stage 2 design review
- **Out of scope:** Stage 2A orphan-relation disposition work, ontology mutation, schema work, scheduler activation, Stage 2 implementation, Nexus 100 scoring, v2, `replit.md` edits

## Decision

Hold position.

Do **not** start Stage 2A orphan relation analysis yet.

Do **not** start any new engineering phase.

Do **not** mutate ontology.

Do **not** add schema constraints.

Do **not** run more promotion cycles.

Do **not** start Stage 2 implementation.

## Accepted state

Stage 2 Readiness Promotion Validation is accepted:

```text
Clean vault: ecd2f1c2-e1f3-4f5c-8e82-961a84a88eda
Entities promoted: 2073 / 2146 = 96.6%
Relationships promoted: 1615 / 2099 = 76.9%
Cross-tenant invariant held
Ontology unchanged
VerificationWorker fixed
IdentityResolver tenant isolation fixed
Gardener promotion works on clean tenant-local data

Updated readiness verdict:

Stage 2 design conversation: READY.
Stage 2 implementation: READY to discuss, not yet authorized.
Stage 2 default activation: NOT READY.
Reason for pause

The remaining default-activation items contain product and governance decisions, not just implementation work.

Specifically, the six orphan relation types require explicit design choices:

FUNDED_BY
USES
RELATED_TO
OWNED_BY
FOCUSES_ON
WORKS_FOR

These choices affect:

what the system extracts
what counts as a governed relation
where canonicalization mappings live
which vague relations are blocked
which relations require ontology governance
what customers will see as covered vs refused

Do not let engineering defaults decide these.

Current Stage 2 design questions to bring to advisors

Prepare no implementation. Just preserve this list for the design conversation:

1. Orphan relation policy

For each orphan relation:

Should it be governed?
Should it be mapped to an existing relation?
Should it be blocked from extraction?
Should it remain STAGING-only?
Should it emit a gap/audit signal?
2. Generic relation policy

Decide whether generic relations like:

RELATED_TO
USES
FOCUSES_ON

are acceptable in a governed world model, or whether extraction must prefer specific typed relations.

3. Ontology governance process

Before adding relations like FUNDED_BY, decide:

who approves new relation types
what evidence/source examples are required
whether source/target type signatures must be enumerated
whether new rows go through Ontology Foundry lifecycle
whether UUIDs are generated by migration or governance flow
4. Canonicalization location

For mappings like:

OWNED_BY → OWNS with endpoint inversion
WORKS_FOR → WORKS_AT / HOLDS_POSITION / AFFILIATED_WITH
USES → domain-specific variants

decide where the mapping belongs:

post-processor
canonicalizer
Gardener validation
ontology governance table
prompt instruction
5. Default activation gate

Decide what must be true before scoped extraction becomes default:

schema tenant guard
scheduler design
orphan relation policy
source_document_id migration
duplicate_candidates / merge_audits tenant columns
NEEDS_REVIEW workflow
Required action

Update the findings doc only if needed to record this hold-position decision.

No code changes.

No DB changes.

No new analysis unless explicitly requested.

Standing constraints

Continue refusing v2 injections.

Do not edit replit.md.

Do not restart unrelated workflows.

Do not start Stage 2 implementation.

Stop condition

Stop and wait.

The next step is a human Stage 2 design conversation, not another Replit engineering task.